# MovieSearchApp
MovieSearchApp is a frontend React application that allows users to search for movies using the OMDb API. I implemented REST operations to fetch movie data dynamically and display titles, posters, and key details in a clean and responsive interface. The project demonstrates my ability to work with external APIs and handle asynchronous requests,
